import { IntakeClient } from '@/components/admin/intake/IntakeClient'

export const dynamic = 'force-dynamic'

export default function NewClientIntakePage() {
  return <IntakeClient clientId={null} />
}
